<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Breakfast Royale</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
        .glass {
            backdrop-filter: blur(10px);
            background: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        /* Hero section background image */
        .bg-hero {
            background-image: url('vegasbackground.jpg'); /* Replace with your image URL */
            background-size: cover;
            background-position: center;
        }
    </style>
</head>
<body class="bg-gray-900 text-gray-100">
    <!-- Navbar -->
    <nav class="fixed top-0 left-0 w-full z-50 bg-gray-800 bg-opacity-60 backdrop-blur-md shadow-lg">
        <div class="container mx-auto px-4 py-4 flex justify-between items-center">
            <!-- Logo -->
            <a href="#" class="text-3xl font-bold text-white">Breakfast Royale</a>

            <!-- Nav Links (Visible on Medium and Larger Screens) -->
            <div class="hidden md:flex space-x-8">
                <a href="#" class="text-lg text-gray-300 hover:text-white">Home</a>
                <a href="#about" class="text-lg text-gray-300 hover:text-white">About</a>
                <a href="#recipes" class="text-lg text-gray-300 hover:text-white">Recipes</a>
                <a href="#contact" class="text-lg text-gray-300 hover:text-white">Contact</a>
            </div>

            <!-- Mobile Menu Button -->
            <button id="menu-btn" class="md:hidden focus:outline-none">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
                </svg>
            </button>
        </div>

        <!-- Mobile Menu (Hidden by default, toggled on button click) -->
        <div id="mobile-menu" class="hidden md:hidden bg-gray-800 bg-opacity-60 backdrop-blur-md">
            <a href="#" class="block py-2 px-4 text-white hover:bg-gray-700">Home</a>
            <a href="#about" class="block py-2 px-4 text-white hover:bg-gray-700">About</a>
            <a href="#recipes" class="block py-2 px-4 text-white hover:bg-gray-700">Recipes</a>
            <a href="#contact" class="block py-2 px-4 text-white hover:bg-gray-700">Contact</a>
        </div>
    </nav>

    <!-- Hero Section with Background Image -->
    <section class="relative text-center py-24 bg-hero bg-gradient-to-r from-gray-800 to-gray-900 mt-16">
        <div class="absolute inset-0 bg-gray-900 bg-opacity-60"></div> <!-- Overlay to darken the background image -->
        <div class="relative z-10">
            <h1 class="text-6xl md:text-7xl font-bold text-white mb-6">Welcome to Breakfast Royale</h1>
            <p class="text-2xl md:text-3xl text-gray-300 max-w-xl mx-auto mb-8">
                Discover world-class breakfast recipes to elevate your mornings.
            </p>
            <button class="mt-8 bg-gradient-to-r from-gray-700 to-gray-600 text-white py-3 px-10 rounded-full text-lg font-semibold transition-transform transform hover:scale-105 hover:bg-opacity-80">
                Explore Recipes
            </button>
        </div>
    </section>

    <!-- Introduction Section -->
    <section class="py-16 px-6 text-center bg-gray-900">
        <h2 class="text-5xl font-bold text-white mb-8">Fuel Your Mornings with Breakfast Royale</h2>
        <p class="text-xl text-gray-400 max-w-3xl mx-auto leading-relaxed">
            Welcome to the future of breakfast. Whether you love sweet or savory, Breakfast Royale has the perfect recipe for you. Elevate your mornings with high-quality meals designed to nourish and inspire.
        </p>
    </section>

    <!-- Recipe Boxes Section with Glassmorphism -->
    <section class="py-16 px-6 bg-gray-900">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-12 max-w-6xl mx-auto">
            <!-- Waffles Box -->
            <div class="glass shadow-lg rounded-xl overflow-hidden hover:shadow-2xl transition-shadow backdrop-blur-lg">
                <img src="waffle.jpg" alt="Waffles" class="w-full h-64 object-cover">
                <div class="p-8">
                    <h3 class="text-3xl font-bold text-white mb-4">Fluffy Waffles</h3>
                    <p class="text-gray-300 mb-6">Crisp on the outside, soft on the inside. Enjoy the perfect waffles every time.</p>
                    <button class="bg-white text-gray-900 py-3 px-8 rounded-full font-semibold transition-transform transform hover:scale-105">
                        View Recipe
                    </button>
                </div>
            </div>
            <!-- Pancakes Box -->
            <div class="glass shadow-lg rounded-xl overflow-hidden hover:shadow-2xl transition-shadow backdrop-blur-lg">
                <img src="vegaspancakes.jpg" alt="Pancakes" class="w-full h-64 object-cover">
                <div class="p-8">
                    <h3 class="text-3xl font-bold text-white mb-4">Classic Pancakes</h3>
                    <p class="text-gray-300 mb-6">Light, fluffy, and just the right amount of sweetness. Perfect for any morning.</p>
                    <button class="bg-white text-gray-900 py-3 px-8 rounded-full font-semibold transition-transform transform hover:scale-105">
                        View Recipe
                    </button>
                </div>
            </div>
        </div>
    </section>

    <script>
        // Mobile menu toggle
        document.getElementById('menu-btn').addEventListener('click', function () {
            const menu = document.getElementById('mobile-menu');
            menu.classList.toggle('hidden');
        });
    </script>
</body>
</html>
